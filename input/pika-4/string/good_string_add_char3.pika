exec {
    var s := "mary".
    var s2 := "had".
    var c := ^a^.
    var s3 := "little".
    var s4 := "lamb".

    var s5 := s + ^ ^ + s2 + ^ ^ + c + ^ ^ + s3 + ^ ^ + s4.

    print s, _n_, s2, _n_, c, _n_, s3, _n_, s4, _n_, s5.
}