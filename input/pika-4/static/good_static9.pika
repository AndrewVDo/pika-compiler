exec {
    var condition := 5.

    while (condition > 0) {
        static var a := 1.

        {
            a := a + 1.
            print a;.
        }
        {
            var a := 69.
            print a;.
        }

        print a, _n_.

        condition := condition - 1.
    }

    print "done".
}
