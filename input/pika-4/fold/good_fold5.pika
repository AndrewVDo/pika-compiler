exec {
    var numbers := [97, 98, 99, 100].
    var catNumber := <string s, int x> -> string { return s + ^ ^ + [ x | char ]. }.
    var numList := numbers fold [ "list:" ] catNumber.


    print numbers, _n_, numList.
}