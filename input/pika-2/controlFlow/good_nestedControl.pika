exec {
    var tries := 10.
    while(tries > 0) {
        if(tries > 3) {
            print "no sweat!", _n_.
        }
        else {
            print "sweating!", _n_.
        }
        tries := tries - 1.
    }
}