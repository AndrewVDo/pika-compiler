exec {
    var numTries := 5.
    while(numTries > 0) {
        print numTries; .
        numTries := numTries - 1.

        if(numTries == 3) {
            break.
        }
    }
    print numTries.
}